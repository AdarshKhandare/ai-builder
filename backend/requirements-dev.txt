# Test dependencies for the Forge backend.
# Install alongside requirements.txt: pip install -r requirements.txt -r requirements-dev.txt
#
# Version floors are deliberately conservative so the suite works on the
# pinned runtime deps (fastapi 0.115, pydantic 2.7+, httpx 0.27+). Latest
# stable releases on 2026-07-03 are pytest 9.1.1, pytest-asyncio 1.4.0,
# httpx 0.28.1 — all backward compatible with these floors.

# Test runner
pytest>=8.0

# Async test support. 0.21+ adds asyncio_mode = auto (used in pytest.ini).
pytest-asyncio>=0.23

# httpx is already a runtime dep (the Zen client uses it) but is repeated
# here because the test client also depends on it: ASGITransport lives in
# httpx >= 0.27. Pinned independently so a future runtime downgrade does
# not silently break tests.
httpx>=0.27
